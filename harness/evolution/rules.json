{
  "schema_version": 1,
  "policy": "Rules are added only after a failure pattern is reproduced, fixed, and reviewed.",
  "rules": [
    {
      "id": "map-schema-invalid",
      "applies_to": ["json-syntax", "context-loader-analysis", "context-loader-smoke"],
      "symptoms": ["Invalid JSON", "missing modules object", "unknown dependency"],
      "response": [
        "Validate the JSON syntax.",
        "Compare the map with the current schema and filesystem.",
        "Correct the smallest inaccurate map entry and rerun the failed check."
      ]
    },
    {
      "id": "verification-command-invalid",
      "applies_to": ["shell-syntax", "required-files"],
      "symptoms": ["command not found", "missing required file", "shell syntax error"],
      "response": [
        "Confirm the command works directly from the repository root.",
        "Update checks.json and the documented command together.",
        "Do not mark an unavailable command as passing."
      ]
    }
  ]
}
